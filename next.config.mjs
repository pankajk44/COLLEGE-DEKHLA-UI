/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['college-dakhla-bucket.blr1.digitaloceanspaces.com'],
      },
};

export default nextConfig;
